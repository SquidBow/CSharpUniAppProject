<Window x:Class="MoneyManager.WpfUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoneyManager.WpfUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Frame x:Name="MainFrame" />
    <!--
     <StackPanel>
     <Grid ColumnDefinitions="*, *">
         <Button Grid.Column="0" Click="AddButton">+</Button>
         <Button Grid.Column="1">-</Button>
     </Grid>
     <ListBox Name="WalletsList" HorizontalContentAlignment="Stretch" MouseDoubleClick="ShowIndividualWallet">
         <ListBox.ItemTemplate>
             <DataTemplate>
                 <Border BorderBrush="Black" BorderThickness="1" Padding="10">
                     <Grid ShowGridLines="False" ColumnDefinitions="20, *, *">
                         <TextBlock Grid.Column="0" Text="{Binding Id}" FontWeight="Bold"/>
                         <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="Bold"/>
                         <TextBlock Grid.Column="2" Text="{Binding Currency}" FontWeight="Bold"/>
                     </Grid>
                 </Border>
             </DataTemplate>
         </ListBox.ItemTemplate>
     </ListBox>
 </StackPanel>
    -->
</Window>
